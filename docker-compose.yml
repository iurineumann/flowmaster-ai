# docker-compose.yml
services:
  # 1. Serviço de Backend (Python/FastAPI)
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend
      - ./init_db.sh:/app/init_db.sh
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env # Garante que TODAS as variáveis (CORS, DB, JWT) sejam carregadas
    environment:
      PYTHONUNBUFFERED: 1
      # DATABASE_URL é carregado pelo env_file

    command: >
      sh -c "
        chmod +x /app/init_db.sh && \
        /app/init_db.sh && \
        echo '🚀 Iniciando Gunicorn...' && \
        /app/.venv/bin/gunicorn backend.main:app --workers 5 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
      "

  # 2. Serviço de Banco de Dados (PostgreSQL)
  db:
    image: postgres:14-alpine
    restart: always
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - ./.data/postgres:/var/lib/postgresql/data/
    healthcheck:
      # ✅ CORREÇÃO: Verifica apenas a conexão do usuário no host
      test:
        [
          "CMD-SHELL",
          "sh -c 'pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}'",
        ]
      interval: 10s
      timeout: 5s
      retries: 5

  # 3. Serviço de Cache (Redis)
  redis:
    image: redis:7-alpine
    container_name: flowmaster-ai-redis-1
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - ./.data/redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 4. Serviço de Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    volumes:
      - ./frontend/dist:/usr/share/nginx/html
      - ./config/nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend

volumes:
  flowmaster-ai-postgres-data:
  flowmaster-ai-redis-data:
